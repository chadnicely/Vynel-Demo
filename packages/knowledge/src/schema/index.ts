// Barrel for the `knowledge` domain schema. Aggregated by the root
// `packages/db/src/schema/index.ts` so drizzle-kit picks up every
// table without per-domain config tweaks.

export * from './sources.js'
export * from './documents.js'
export * from './chunks.js'
