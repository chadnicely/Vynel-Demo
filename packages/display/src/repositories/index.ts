// Internal re-export surface of the display repositories — imported as a
// namespace by the ops (`import * as displayWidgetsRepository`). Not part of
// the package's public API; consumers use `src/index.ts`.

export * from './display-widgets.js'
