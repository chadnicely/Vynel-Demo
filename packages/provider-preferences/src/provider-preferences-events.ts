// Outbox events for provider-preferences — intentionally EMPTY. Setting a
// default provider publishes no cross-feature signal in Phase 1. Kept as a
// documented placeholder so the events surface has one home when it lands.
export {}
