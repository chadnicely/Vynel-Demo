// The `@vynel/ssh-servers/mcp` subpath — the SDK-touching half (the asks
// barrel-split precedent). Turn streams dynamic-import this.

export {
  buildSshFeatureDescriptor,
  SSH_PROMPT_INSTRUCTIONS,
} from './ssh-mcp-feature-descriptor.js'
