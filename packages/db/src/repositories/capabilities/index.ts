// Repository barrel for the `capabilities` domain. One repo today
// (workspace-capabilities); the barrel is required for the
// `@vynel/db/repositories/capabilities` subpath export (db package exports map).
export * from './workspace-capabilities.js'
