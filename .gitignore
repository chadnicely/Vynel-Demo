# dependencies
node_modules/
.pnpm-store/

# build output
dist/
.vite/
.turbo/
*.tsbuildinfo

# tests
coverage/

# local data & secrets
.data/
# downloaded ONNX voice models (STT/TTS) — fetched via `pnpm voice:fetch-models`
.models/
*.db
*.db-shm
*.db-wal
.env
.env.*
!.env.example
.vynel/

# local model caches / large binaries
*.onnx

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# rust (apps/desktop/src-tauri)
target/

# parallel-pull agent worktrees (fan-out scratch; branches merge back, trees don't)
.claude/worktree/
.claude/worktrees/

# playwright-cli browser-smoke artifacts (screenshots/snapshots)
.playwright-cli/
