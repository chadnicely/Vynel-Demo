# .claude/ — Claude Code workspace for Vynel

- **`ceo/soul.md`** — load this to work on Vynel: your role, *why we left the old repo and are here*,
  and the continuous-check discipline (direction · repetition→package · small→big · learn & correct).
- **`rules/`** — the per-move operating cadence (`build-discipline.md`). Code/style rules live in the
  root `CLAUDE.md`.
- **`journal/`** — the build log; one short entry per module move.

**Read order at the start of a session:** `CLAUDE.md` → `docs/vision.md` → `docs/architecture.md` →
`docs/restructure-research.md` → `.claude/ceo/soul.md`.

*(Commands and agents from the old repo — `/build-domain`, `/finalize-domain`, `code-reviewer` — get
ported and adapted as we pull the code that needs them, not before.)*
