// Placeholder root. Real entry points live under
// scripts/src/generators/ (sdk + mcp + schema-parity codegen),
// scripts/src/migrations/ (Phase 1 → Phase 2 data movers),
// scripts/src/debug/ (throwaway tools).
export {}
