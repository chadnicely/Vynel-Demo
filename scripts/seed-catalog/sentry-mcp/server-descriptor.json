{
  "note": "This mcp item installs by writing one entry into the scope's Claude MCP config (config-is-truth). It declares auth: oauth — install writes the entry credential-less, and Connect drives the native `claude mcp login sentry` browser flow; the credential lands in Claude's own store, never in Vynel. The desktop never downloads this artifact; the registry's publish schema simply requires one, and this descriptor doubles as the human-readable record of what installing writes.",
  "server": {
    "serverName": "sentry",
    "transport": "http",
    "url": "https://mcp.sentry.dev/mcp",
    "auth": { "type": "oauth" }
  }
}
