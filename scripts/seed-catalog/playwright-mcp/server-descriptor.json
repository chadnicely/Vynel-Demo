{
  "note": "This mcp item installs by writing one entry into the scope's Claude MCP config (config-is-truth): user scope lands in ~/.claude.json mcpServers, workspace scope in <workspace>/.mcp.json — locations native Claude tooling reads directly. The desktop never downloads this artifact; the registry's publish schema simply requires one, and this descriptor doubles as the human-readable record of what installing writes.",
  "server": {
    "serverName": "playwright",
    "transport": "stdio",
    "commandOrUrl": "npx",
    "args": ["@playwright/mcp@latest"],
    "environment": {}
  }
}
