{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "jarvis-window",
  "description": "The Jarvis overlay controls its own window: show on wake, hide on session end, park bottom-right, drag to move.",
  "windows": ["jarvis"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-set-position",
    "core:window:allow-start-dragging"
  ]
}
